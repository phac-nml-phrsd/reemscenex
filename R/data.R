#' Model fitted to synthetic data
#'
#' From main `reem` package vignette
#'
#' @format ## `model.example`
#' A [reem::reem-class] object
#' @source `obj` from first chunk of Forecasting section in https://github.com/phac-nml-phrsd/reem/README.Rmd on commit d28d55ae6f8dc38b99c6f3040efd5c1f1ce22b8b. When (re-)generating `obj`, be sure to install the latest version of `reem` on disk, run the `reem` vignette, save `obj` to disk, load `obj` into `model.example` in the `reemscenex` repo, `update_data("model.example", overwrite=T), then update the version of `reem` in the `reemscenex` repo, specifying a new minimum version 
#' @autoglobal
"model.example"
